#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){

    int n;
    cin>>n;
    vector<int>arr(n);
    arr.push_back(5);
    arr.push_back(6);

    arr.pop_back();
    arr.size();
    arr.empty();

    
    return 0;
}