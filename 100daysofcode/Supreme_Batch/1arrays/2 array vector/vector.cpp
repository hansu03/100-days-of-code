#include<bits/stdc++.h>
using namespace std;
int main(){
    //declaration 
    vector<int>arr;

    //initialization
    int n;
    cin>>n;
    vector<int>arr(n);
    vector<int>brr(10);

    //insertion in vector
    vector<int>arr;
    arr.push_back(5);
    arr.push_back(6);
    arr.push_back(7);

    //deletion in array
    arr.pop_back();

    //vector size and capacity
    arr.size();
    arr.capacity();


    //vector is empty or not
    arr.empty();


    

}